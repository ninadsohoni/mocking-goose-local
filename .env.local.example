# Local Development Environment Variables
# Copy this file to .env.local and fill in your values

# Databricks Configuration (required for Goose to work)
# You'll enter these in the web UI, but you can also set them here for testing
# DATABRICKS_HOST=https://your-workspace.cloud.databricks.com/
# DATABRICKS_TOKEN=dapi...

# Local paths (adjust based on your setup)
# Path to the mock-and-roll directory (defaults to ./mock-and-roll)
LOCAL_MOCK_AND_ROLL_DIR=${PWD}/mock-and-roll

# Path to directory containing the goose binary (defaults to ~/.local/bin)
GOOSE_BIN_DIR=${HOME}/.local/bin

# Optional: Direct path to goose binary (overrides GOOSE_BIN_DIR)
# GOOSE_BIN=${HOME}/.local/bin/goose

# App Configuration
APP_HOST=0.0.0.0
APP_PORT=8000

# Session Configuration
INACTIVITY_TIMEOUT_SECONDS=3600  # 1 hour
COOKIE_MAX_AGE_SECONDS=28800     # 8 hours

# Security (set to true if running behind HTTPS proxy)
COOKIE_SECURE=false
